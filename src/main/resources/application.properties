#spring.datasource.url=jdbc:postgresql://localhost:5432/crud
#spring.datasource.username=postgres
#spring.datasource.password=admin
#spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.properties.hibernate.format_sql=true

# application.properties
#spring.mvc.cors.allowed-origins=http://localhost:5173
#spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE

# DataSource settings: Oracle
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=SYSTEM
spring.datasource.password=SYS
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.jpa.hibernate.ddl-auto=update

# Specify the DBMS
spring.jpa.database = ORACLE
# Create/update tables automatically
spring.jpa.generate-ddl = true

# Show or not log for each sql query
spring.jpa.show-sql = true





# Other properties (optional)
# spring.datasource.hikari.* # HikariCP connection pool properties if using HikariCP

# Additional properties specific to your application or Oracle setup can be added as needed.
